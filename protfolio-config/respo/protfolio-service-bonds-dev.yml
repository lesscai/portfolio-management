spring:
  application:
    name: protfolio-service-bonds
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/psmsdb?useUnicode=true&characterEncoding=utf-8&useSSL=false
      username: root
      password: admin
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      driver-class-name: com.mysql.jdbc.Driver

server:
  port:  9051
